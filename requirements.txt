# Dastak — PSX Intelligence Layer
# Zero paid APIs required.
# Windows-safe: all packages have pre-built wheels, no C++ compiler needed.

# HTTP & scraping
httpx==0.27.0
beautifulsoup4==4.12.3
lxml>=5.3.0               # Python 3.13 binary wheel support

# Data
pandas>=2.2.3             # Python 3.13 binary wheel support
python-dateutil==2.9.0

# PDF parsing (for filings)
pymupdf>=1.24.12          # Python 3.13 binary wheel support

# LLM — free tiers only
groq==0.9.0
google-generativeai==0.7.2
ollama==0.2.1

# Embeddings — runs locally, no key
sentence-transformers==3.0.1

# Agent orchestration
langgraph>=0.1.14
langchain-core>=0.2.22    # Updated to satisfy langgraph requirement

# API layer
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Storage
sqlite-utils==3.36

# CLI / dev
rich==13.7.1
python-dotenv==1.0.1
typer==0.12.3